# include <stdio.h>
#include<conio.h>
# define bool int
void main();
{
int l, r;
quickSort(A, 0, arr_size-1);
1 = 0;
r = arr_size-1; 
while (l < r)
{
if(A[l] + A[r] == sum)
return 1; 
if(A[l] + A[r] < sum)
l++;
A[i] + A[j] > sum
r--;
}    
getch();
}
